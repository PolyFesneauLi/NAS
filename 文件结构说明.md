# NAS系统文件结构说明

## 目录结构

```
NAS/
├── scripts/                    # 脚本文件目录
│   ├── start-nas.bat          # 启动脚本
│   ├── stop-nas.bat           # 停止脚本
│   ├── check_port.bat         # 端口检查脚本
│   ├── quick-restart.bat      # 快速重启脚本
│   ├── test-mongodb.bat       # MongoDB测试脚本
│   └── test-scripts.bat       # 脚本测试工具
├── client/                     # 前端代码
├── server/                     # 后端代码
├── storage/                    # 文件存储目录
├── config/                     # 配置文件目录
├── create-desktop-shortcuts.bat # 桌面快捷方式创建（已移动到scripts目录）
├── ico_blue.ico               # 启动图标（蓝色）
├── ico_red.ico                # 停止图标（红色）
├── ico_green.ico              # 备用图标（绿色）
└── README.md                   # 项目说明文档
```

## 脚本文件说明

### 主要脚本

1. **start-nas.bat** (scripts目录)
   - 功能：启动NAS系统
   - 图标：ico_blue.ico
   - 桌面快捷方式：Start NAS System.lnk

2. **stop-nas.bat** (scripts目录)
   - 功能：停止NAS系统
   - 图标：ico_red.ico
   - 桌面快捷方式：Stop NAS System.lnk

### 辅助脚本

3. **check_port.bat** (scripts目录)
   - 功能：检查端口状态
   - 用途：诊断端口冲突问题

4. **quick-restart.bat** (scripts目录)
   - 功能：快速重启系统
   - 用途：解决端口冲突

5. **test-mongodb.bat** (scripts目录)
   - 功能：测试MongoDB连接
   - 用途：诊断数据库问题

6. **test-scripts.bat** (scripts目录)
   - 功能：测试脚本语法
   - 用途：验证脚本正确性

### 根目录入口

7. **create-desktop-shortcuts.bat** (scripts目录)
   - 功能：创建桌面快捷方式
   - 用途：为用户提供便捷的启动和停止入口

## 图标文件说明

- **ico_blue.ico**: 启动脚本图标（蓝色）
- **ico_red.ico**: 停止脚本图标（红色）
- **ico_green.ico**: 备用图标（绿色）

## 使用方法

### 桌面快捷方式

1. 运行 `create-desktop-shortcuts.bat` 创建桌面快捷方式
2. 双击 "Start NAS System" 启动系统
3. 双击 "Stop NAS System" 停止系统

### 命令行使用

```bash
# 直接调用脚本
scripts\start-nas.bat
scripts\stop-nas.bat

# 诊断工具
scripts\check_port.bat
scripts\quick-restart.bat
scripts\test-mongodb.bat

# 创建桌面快捷方式
scripts\create-desktop-shortcuts.bat
```

## 路径更新说明

### 已更新的路径引用

1. **create-desktop-shortcuts.bat**
   - 更新了脚本路径指向scripts目录
   - 更新了图标路径指向新的图标文件

2. **scripts/start-nas.bat**
   - 更新了停止脚本引用为stop-nas.bat

3. **scripts/quick-restart.bat**
   - 更新了脚本调用路径

4. **scripts/check_port.bat**
   - 更新了脚本引用路径

5. **README.md**
   - 更新了所有脚本路径引用

### 兼容性

- 根目录的start.bat和stop.bat保持向后兼容
- 桌面快捷方式会自动更新路径
- 所有脚本调用都已更新为新的路径结构

## 注意事项

1. **路径依赖**: 所有脚本都假设在NAS根目录运行
2. **图标文件**: 确保ico_blue.ico和ico_red.ico文件存在
3. **权限要求**: 某些操作可能需要管理员权限
4. **编码问题**: 所有脚本使用UTF-8编码，避免中文乱码

## 故障排除

如果遇到路径问题：

1. 确保在NAS根目录运行脚本
2. 检查scripts目录是否存在
3. 验证图标文件是否存在
4. 重新运行create-desktop-shortcuts.bat更新快捷方式 