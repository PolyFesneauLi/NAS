# 分布式NAS系统 - 启动问题解决指南

## 🚨 后端启动失败问题

### 问题描述
点击"启动NAS系统"后，后端服务没有正常启动。

### 解决方案

#### 方案1: 运行诊断脚本
```bash
# 运行诊断脚本，自动检查问题
diagnose-startup.bat
```

#### 方案2: 使用简化启动脚本
```bash
# 使用简化版启动脚本（推荐）
start-nas-simple.bat
```

#### 方案3: 测试模式启动
```bash
# 不依赖MongoDB的测试模式
start-nas-no-db.bat
```

#### 方案4: 手动启动
```bash
# 手动启动服务器
cd server
npm install
npm start

# 新开终端启动客户端
cd client
npm install
npm start
```

## 🔍 常见问题排查

### 1. Node.js环境问题
**症状**: 提示"node不是内部或外部命令"
**解决**: 
- 下载安装Node.js: https://nodejs.org/
- 确保Node.js版本 >= 16.0.0
- 重启命令行窗口

### 2. 依赖包问题
**症状**: 提示"模块未找到"
**解决**:
```bash
# 安装服务器依赖
cd server
npm install

# 安装客户端依赖
cd client
npm install
```

### 3. 端口占用问题
**症状**: 提示"端口已被占用"
**解决**:
```bash
# 查看端口占用
netstat -ano | findstr :5000
netstat -ano | findstr :3000

# 停止占用进程
taskkill /f /pid <进程ID>
```

### 4. MongoDB连接问题
**症状**: 提示"MongoDB连接失败"
**解决**:
- 启动MongoDB服务
- 或使用测试模式: `start-nas-no-db.bat`
- 或修改.env文件中的MongoDB URI

### 5. 权限问题
**症状**: 提示"权限不足"
**解决**:
- 以管理员身份运行
- 检查文件夹权限
- 确保有写入权限

## 📋 启动脚本对比

| 脚本名称 | 特点 | 适用场景 |
|---------|------|----------|
| `start-nas-simple.bat` | 简化版，自动安装依赖 | 推荐使用 |
| `start-nas-no-db.bat` | 不依赖MongoDB | 测试环境 |
| `start-nas.bat` | 原始版本 | 完整功能 |
| `quick-start.bat` | 开发者工具 | 高级配置 |

## 🛠️ 故障排除步骤

### 步骤1: 环境检查
```bash
# 检查Node.js
node -v

# 检查npm
npm -v

# 检查项目结构
dir server
dir client
```

### 步骤2: 依赖检查
```bash
# 检查服务器依赖
cd server
npm list

# 检查客户端依赖
cd client
npm list
```

### 步骤3: 端口检查
```bash
# 检查端口占用
netstat -an | findstr ":5000"
netstat -an | findstr ":3000"
```

### 步骤4: 日志检查
```bash
# 查看启动日志
type logs\*.log

# 查看错误日志
type logs\*-error.log
```

## 📞 获取帮助

### 1. 查看日志文件
- 启动日志: `logs\startup.log`
- 错误日志: `logs\*-error.log`
- 系统日志: `logs\*.log`

### 2. 运行诊断工具
```bash
# 自动诊断
diagnose-startup.bat

# 测试VBScript
test-vbs.bat

# 最终测试
final-test.bat
```

### 3. 检查配置文件
- 环境变量: `.env`
- 服务器配置: `server\config.js`
- 客户端配置: `client\package.json`

## 🎯 快速修复

### 立即解决
1. 运行 `diagnose-startup.bat`
2. 根据诊断结果修复问题
3. 使用 `start-nas-simple.bat` 启动

### 备用方案
1. 使用 `start-nas-no-db.bat` 测试模式
2. 手动启动服务器和客户端
3. 检查MongoDB服务状态

---

**如果问题仍然存在，请提供以下信息：**
- 错误信息截图
- 诊断脚本输出
- 系统环境信息
- 日志文件内容 