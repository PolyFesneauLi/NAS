# NAS系统操作说明

## 快速启动

### 方法一：使用桌面快捷方式（推荐）
1. 双击桌面上的"启动NAS系统"图标
2. 系统会自动启动服务器和客户端
3. 浏览器会自动打开 http://localhost:3000

### 方法二：使用批处理文件
1. 双击 `start-nas.bat` 文件
2. 等待系统启动完成
3. 浏览器会自动打开

### 方法三：手动启动
1. 打开命令行，进入项目目录
2. 运行 `npm start` 启动服务器
3. 进入 `client` 目录，运行 `npm start` 启动客户端

## 停止系统
- 双击桌面上的"停止NAS系统"图标
- 或运行 `stop.bat` 文件

## 界面配置

### 显示用户信息框
在 `server/.env` 文件中设置：
```
SHOW_USER_INFO=true  # 显示用户信息框
SHOW_USER_INFO=false # 隐藏用户信息框（默认）
```

### 文件列表高度
- Admin用户：页面高度的60%
- 普通用户：页面高度的80%

### 上传文件位置
- 上传文件组件已移动到文件列表下方
- 文件列表和上传文件之间有30px间距
- 只有Admin用户可以看到上传功能

### 滚动条优化
- 滚动条在不滚动时半透明显示
- 搜索和排序组件固定在顶部，不随表格滚动
- 表格头部在滚动时保持可见
- 表格容器最大高度设置为70vh，支持垂直和水平滚动

## 功能说明

### 文件管理
- **上传文件**：支持拖拽或点击选择文件
- **下载文件**：点击文件名即可下载
- **删除文件**：选中文件后点击删除按钮
- **批量操作**：Admin用户可以批量删除文件
- **文件夹导航**：支持多级文件夹浏览

### 文件排序
- 按上传时间排序
- 按文件大小排序
- 按文件名排序
- 按文件扩展名排序

### 搜索功能
- 支持文件名搜索
- 实时搜索结果

## 用户权限

### Admin用户
- 可以上传、下载、删除文件
- 可以创建文件夹
- 可以批量删除文件
- 可以查看存储使用情况

### 普通用户
- 只能下载文件
- 可以浏览文件列表
- 可以搜索文件

## 故障排除

### 端口冲突
系统会自动检测端口占用并分配新端口：
- 服务器端口：5000, 5001, 5002...
- 客户端端口：3000, 3001, 3002...

### 启动失败
1. 检查MongoDB是否运行
2. 检查端口是否被占用
3. 查看控制台错误信息

### 文件上传失败
1. 检查文件大小是否超限
2. 检查文件类型是否支持
3. 检查网络连接

## 技术支持
如遇到问题，请查看：
- `启动问题解决指南.md`
- `用户使用指南.md`
- 控制台错误信息 