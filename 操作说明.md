# NAS系统操作说明

## 🚀 快速启动

### 方法1：直接运行
```bash
# 双击运行
start-nas.bat
```

### 方法2：创建桌面图标
```bash
# 1. 运行创建桌面图标
create-desktop-shortcuts.bat

# 2. 双击桌面上的"启动NAS系统"图标
```

## 📋 功能特点

✅ **智能检测**：如果程序已运行，直接打开浏览器  
✅ **自动端口分配**：端口被占用时自动选择新端口  
✅ **快速启动**：已运行时不重复启动，秒开  
✅ **自动配置**：首次运行自动创建配置文件  

## 🛠️ 操作步骤

### 首次使用
1. 确保已安装 Node.js (版本 >= 16.0.0)
2. 运行 `start-nas.bat`
3. 等待系统启动（首次启动需要安装依赖）
4. 浏览器自动打开 `http://localhost:3000`

### 日常使用
1. 双击桌面图标或运行 `start-nas.bat`
2. 系统自动检测状态：
   - 如果已运行：直接打开浏览器
   - 如果未运行：自动启动并打开浏览器

### 停止系统
```bash
# 运行停止脚本
stop.bat
```

## 🔧 故障排除

### 如果启动失败
1. 检查 Node.js 是否安装：`node -v`
2. 检查端口是否被占用：`netstat -an | findstr ":5000"`
3. 运行诊断脚本：`diagnose-startup.bat`

### 如果端口被占用
- 系统会自动分配新端口
- 浏览器会自动跳转到新端口
- 无需手动操作

## 📁 文件说明

| 文件 | 说明 |
|------|------|
| `start-nas.bat` | 主启动脚本（推荐使用） |
| `stop.bat` | 停止服务脚本 |
| `create-desktop-shortcuts.bat` | 创建桌面图标 |
| `diagnose-startup.bat` | 启动问题诊断 |

## ⚠️ 注意事项

1. **首次启动**：需要安装依赖，请耐心等待
2. **端口占用**：系统会自动处理，无需手动干预
3. **MongoDB**：首次使用会自动创建本地数据库
4. **权限**：确保对项目文件夹有读写权限

## 🎯 使用技巧

- **快速访问**：创建桌面图标，一键启动
- **状态检查**：如果程序已运行，会直接打开浏览器
- **自动恢复**：如果部分服务停止，会自动重启
- **端口管理**：自动处理端口冲突，无需手动配置

---

**现在你只需要一个文件：`start-nas.bat`**

双击即可启动整个NAS系统！ 